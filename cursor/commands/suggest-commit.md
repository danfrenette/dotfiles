Review the git diff and suggest a commit message and description that describes
what the code accomplishes.

The output of the message must be in raw, escaped markdown that can be copied
easily into a terminal.

Do not include “git commit” commands, only show the message and description. 
 
Make reasonable assumptions about what the business logic behind the code is, or
ask questions before proceeding if needed.

Do not write a conventional commit message, as this will be formatted by a commitizen
plugin anyways.

Sacrifice grammar for the sake of concision. 