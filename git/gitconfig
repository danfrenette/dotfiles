[user]
  name = Dan Frenette
  email = dan.r.frenette@gmail.com
[init]
  defaultBranch = main
[push]
  default = current
  autoSetupRemote = true
[color]
  ui = true
  diff = auto
  status = auto
  branch = auto
[alias]
  aa = add --all
  ap = add --patch
  branches = for-each-ref --sort=-committerdate --format=\"%(color:blue)%(authordate:relative)\t%(color:red)%(authorname)\t%(color:white)%(color:bold)%(refname:short)\" refs/remotes
  ci = commit -v
  co = checkout
  lol = log --pretty='format:%h %cs%Cblue%d%Creset %s' -40
  st = status
  sl = log --oneline --decorate -20
  sla = log --oneline --decorate --graph --all -20
  slp = log --oneline --decorate
  slap = log --graph --all --decorate --pretty=format:\"%Cblue%d %Cred%h%Creset . %an: %s %Cgreen(%cr)%Creset\" --abbrev-commit --date=relative
  glog = log -E -i --grep
  br = branch
  unstage = reset
  ###
  cp = cherry-pick
  cpa = cherry-pick --abort
  cpc = cherry-pick --continue
  ###
  rb = rebase
  rba = rebase --abort
  rbc = rebase --continue
  rbi = rebase --interactive
  rbs = rebase --skip
  ###
  pfl = push --force-with-lease
  pr = pull --rebase
  ###
  fuck = reset HEAD@{1}
  uncommit = reset --soft HEAD^
  standup = log --since=yesterday --author=\"Dan Frenette\"
  conflicts = diff --name-only --diff-filter=U
  cleanup-merged = "!f() { default_branch=$(git symbolic-ref --quiet --short refs/remotes/origin/HEAD 2>/dev/null); default_branch=${default_branch#origin/}; current_branch=$(git branch --show-current); git for-each-ref --format='%(refname:short)' --merged=HEAD refs/heads | while IFS= read -r branch; do if [ \"$branch\" != \"${default_branch:-main}\" ] && [ \"$branch\" != \"$current_branch\" ]; then git branch -d \"$branch\"; fi; done; }; f"
  copysha = !git rev-parse HEAD | pbcopy
  ###
  df = diff --ignore-space-change --histogram --find-renames --find-copies --unified=10 --stat
[core]
  autocrlf = input
  editor = nvim
  pager = delta
[interactive]
  diffFilter = delta --color-only
[merge]
  ff = only
  tool = nvimdiff
  conflictStyle = zdiff3
[delta]
  navigate = true  # use n and N to move between diff sections
  dark = true      # or light = true, or omit for auto-detection
[commit]
  template = ~/.gitmessage
[pull]
  ff = only
[fetch]
  prune = true
[rebase]
  autosquash = true
  autoStash = true
[branch]
  sort = -committerdate
[diff]
  algorithm = histogram
[help]
  autocorrect = 1
[include]
  path = ~/.gitconfig.local
[rerere]
  enabled = 1
[github]
  user = danfrenette
